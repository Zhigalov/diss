\chapter{Численные эксперименты}

Перед выполнением экспериментальной части работы была произведена
серия численных экспериментов, позволивших решить следующие задачи:

\begin{itemize}
\item обнаружение низкочастотных колебаний ОПД, проверка уравнения~(\ref{eq:hpd_freq_2});
\item нахождение оптимального способа возбуждения и наблюдения колебаний;
\item изучение влияния спиновой релаксации и пространственной неоднородности на частоту колебаний;
\item изучение влияния объемного $^3$He, находящегося в зазорах между аэрогелем и стенками ячейки.
\end{itemize}

\section{Постановка эксперимента, образование ОПД}

Уравнения спиновой динамики~(\ref{eq:leggett_tak}), записанные во
вращающейся системе координат, решались численно в одномерной
геометрии (пространственные неоднородности и спиновые токи допускались
лишь вдоль одной оси). Для счета использовалась модификация
программы на Фортране, написанной В.~В.~Дмитриевым. Программа
использует стандартный алгоритм 540R (PDECOL) для решения систем
дифференциальных уравнений~(\cite{1979_pdecol}, \cite{1992_pdecol}).

В качестве граничных условий было выбрано требование равенства нулю
спиновых токов на границах ячейки. Для рассчета выбраны параметры,
соответствующие реальным экспериментальным условиям:

\begin{itemize}
\item длина ячейки: 18~мм;
\item магнитное поле: 284~Э;
\item градиент магнитного поля: 0.1~Э/см;
\item поле радиочастотной накачки: 1...10~мЭ;
\item температура: 0.7...0.9~$T/T_c$;
\item давление: 19.5~бар, 24.8~бар.
\end{itemize}

В зависимости от температуры и давления выбирались величины
леггеттовской частоты (в соответствии с~\cite{1989_he3b_nmr}),
скорости спиновых волн и коэффициента спиновой диффузии (в
соответствии с~\cite{1987_hpd} и~\cite{1992_hpd_osc}), параметра
релаксации Леггетта-Такаги (в соответствии с~\cite{vw}). Кроме того,
эти параметры могли варьироваться и независимо.

Вычисления практически соответствуют проведению реального эксперимента:
к ячейке приложено постоянное магнитное поле, линейно растущее вдоль оси
$z$, и постоянное (во вращающейся системе координат) поле радиочастотной
накачки, направленное вдоль оси $x$. Частота накачки плавно
увеличивается, так чтобы точка, в которой эта частота равна
ларморовской, проходила через ячейку. При этом в <<нижней>> части ячейки
(в области меньшего поля) образуется ОПД, который постепенно заполняет
всю ячейку.

\image{p!}{num_hpd}{Графики изменения намагниченности и параметра
порядка при образовании ОПД. По горизонтальной оси отложен сдвиг
частоты накачки от ларморовского значения в центре ячейки. Сдвиг
частоты увеличивается со временем, при этом в ячейке образуется ОПД. В
заштрихованной области точка, в которой частота равна ларморовской,
проходит через ячейку. Для каждого параметра показано шесть кривых,
соответствущих значениям в шести точках ячейки. Параметры численного
эксперимента соответствуют давлению 19.5~бар, температуре 0.9~$T_c$,
магнитному полю 284~Э, градиенту поля 0.1~Э/см, амплитуде
радиочастотной накачки 3~мЭ.}

Графики изменения намагниченности и параметра порядка в разных частях
ячейки показаны на рис.~\ref{image:num_hpd}. По горизонтальной оси
отложен сдвиг частоты накачки от ларморовского значения в центре
ячейки. Линия проходится слева направо, в сторону увеличения сдвига
частоты, при этом в ячейке образуется ОПД. В заштрихованной области
точка, в которой частота равна ларморовской, проходит через ячейку.
Для компонент намагниченности~$\bf M$ и вектора~$\bf n$, а также для
угла~$\theta$ показано шесть кривых, соответствущих значениям в шести
точках ячейки вдоль оси $z$.

Выбор начала отсчета для сдвига частоты накачки достаточно произволен. В
формулах предыдущей главы рассматривалась пространственно-однородная
геометрия, в которой сдвиг частоты четко определен. В численных (так же
как и в реальных) экспериментах ОПД создается в присутствии градиента
магнитного поля. При этом возникает пространственно-неоднородная
система, части которой связаны спиновыми токами.  Дополнительную
неоднородность создают стенки ячейки. (Так на рис.~\ref{image:num_hpd}
видно, что полное заполнение ячейки ОПД происходит в момент, когда
частота накачки еще не достигла ларморовского значения на верхнем краю
ячейки, граница ОПД раньше времени <<прилипает>> к стенке ячейки.) В этой
ситуации можно лишь отсчитывать сдвиг от какого-то удобного значения,
понимая, что измеренные величины получаются некоторым сложным
усреднением вкладов от частей ячейки с различными сдвигами.

\section{Возбуждение колебаний ОПД}

После того, как ячейка полностью заполняется доменом, сканирование
частоты останавливается и производится возбуждение колебаний. Подходящим
способом возбуждения оказалось небольшое ступенчатое изменение
магнитного поля вдоль оси $z$ или, что практически эквивалентно, частоты
радиочастотной накачки. Колебания поперечной намагниченности
записываются и аппроксимируются экспонентциально спадающей синусоидой.
Таким образом можно получить зависимости частоты и коэффициента
затухания колебаний от различных параметров.

Было показано, на частоту и коэффициент затухания колебаний не влияет
спиновая диффузия~$D$ (в диапазоне, соответствующем исследуемым
температурам и давлениям), градиент магнитного поля, по крайней
мере, до величины 0.2~Э/см, а также неоднородность радиочастотного
поля, по крайней мере, до величины 10\%.

Таким образом, параметрами, влияющими на результат были амплитуда
радиочастной накачки~$H_R$, сдвиг частоты~$\Delta f$, скорость
спиновых волн~$c_\parallel$, леггеттовская частота~$\Omega_B$ и
время релаксации Леггетта-Такаги~$\tau$.

\section{Частота колебаний}

На рис.~\ref{image:num_f_d_h} показана зависимость частоты колебаний
ОПД от сдвига частоты при различных амплитудах радиочастотной накачки.
Частота нормирована на теоретическое значение, не зависящее от сдвига,
полученное по формуле~(\ref{eq:hpd_freq_2}).


\image{ht!}{num_f_d_h}{Зависимость частоты колебаний ОПД от сдвига
частоты при различных амплитудах радиочастотной накачки. Частота
нормирована на теоретическое значение, полученное по
формуле~(\ref{eq:hpd_freq_2}). Заштрихована область, в которой частота
накачки равна ларморовской частоте внутри ячейки. Параметры численного
эксперимента соответствуют давлению 19.5~бар, температуре 0.9~$T_c$,
магнитному полю 284~Э, градиенту поля 0.1~Э/см.}


В достаточно большом диапазоне сдвигов ($\sim$ 200..500~Гц)
частота колебаний с хорошей точностью совпадает с теорией. На практике
всегда можно измерять частоту колебаний в этом диапазоне и
пользоваться формулой~(\ref{eq:hpd_freq_2}).

Резкое отклонение от теоретического значения при малых сдвигах частоты
связано, судя по всему, с нарушением условия $d \gg bh$, которое мы
предполагали, получая приближенную формулу~(\ref{eq:hpd_freq_2}) для
корня уравнения~(\ref{eq:hpd_freq_eq}). Точное рассмотрение этого
отклонения осложнено неоднородностью сдвига частоты по длине ячейки. В
частности, при достаточно малом сдвиге частоты колебания ОПД
перестают быть пространственно-однородными.

Отклонение от теоретического значения при больших сдвигах частоты
зависит от многих параметров: амплитуды накачки, леггеттовской
частоты, времени релаксации Леггетта-Такаги, скорости спиновых волн.
Конкретный вид этой зависимости довольно сложен и подробно не
исследовался.

\image{ht!}{num_hdep}{Зависимость квадрата частоты колебаний ОПД от
амплитуды радиочастотной накачки при различных температурах. Линии~---
теоретические зависимости, вычисленные по
формуле~(\ref{eq:hpd_freq_2}). Параметры численного эксперимента
соответствуют давлению 19.5~бар, магнитному полю 284~Э, градиенту поля
0.1~Э/см. Сдвиг частоты накачки от ларморовского значения в центре
ячейки равен 224~Гц.}

На рис.~\ref{image:num_hdep} показана зависимость квадрата частоты
колебаний ОПД от амплитуды радиочастотной накачки при различных
температурах. Сдвиг частоты равен 224~Гц. Линиями изображены
теоретические зависимости~(\ref{eq:hpd_freq_2}), вычисленные без
подгоночных параметров. Видно, что частота колебаний при указанном
сдвиге частоты хорошо согласуется с теорией.


\section{Коэффициент затухания колебаний}

Зависимость коэффициента затухания колебаний ОПД от исследуемых
параметров оказалась достаточно простой. Если исключить область
малых сдвигов частоты ($\Delta f<200$~Гц), коэффициент затухания
зависит от сдвига линейно. Точка, в которой эта зависимость
обращается в ноль, 

ячейки, с учетом небольшой поправки, связанной с амплитудой поля
радиочастотной накачки~$H_R$. Коэффициент пропорциональности зависит
только от леггеттовской частоты ~$\Omega_B$ и времени релаксации
Леггетта-Такаги~$\tau$. Интерполяцией результатов численного
эксперимента была получена следующая формула для коэффициента
затухания:

\begin{equation}
k=
  \left(0.1699 - 0.4187 \left(2\pi\Omega_B\cdot 10^{-6}\right)^2 \right)
  \frac{10^{-6}}{\tau} (\Delta f - 0.1037 \gamma H_R)
\label{eq:legg}
\end{equation}

На рис.~\ref{image:num_k} показана зависимость коэффициента затухания
колебаний ОПД. от сдвига частоты при различных температурах. Линиями
изображены зависимости, вычисленные по формуле~(\ref{eq:num_k}).

\image{ht!}{num_k_d_t}{Зависимость коэффициента затухания колебаний ОПД 
от сдвига частоты при различных температурах. Линии~--- зависимости,
вычисленные по формуле~(\ref{eq:num_k}). Параметры численного
эксперимента соответствуют давлению 19.5~бар, магнитному полю 284~Э,
градиенту поля 0.1~Э/см. Амплитуда радиочастотной накачки~--- 3~мЭ.}


\section{Учет влияния объемного гелия в зазорах}

\medskip

Еще одна задача численного эксперимента состояла в оценке влияния
объемного $^3$He в зазорах при наблюдении колебаний ОПД в аэрогеле.
Рассматривается следующая модель: в центре ячейки имеется область,
леггеттовская частота в которой отличается от леггеттовской частоты на
краях. Переходные области имеют форму распределения Ферми с шириной
0.55\% длины ячейки.

Частота колебаний ОПД зависит от леггеттовской частоты, однако за счет
взаимодействия ОПД колеблется на одной, <<усредненной>> частоте.
Изучалась зависимость этой усредненной частоты от доли <<аэрогеля>> в
ячейке и от частот колебаний в леггеттовской частоты в <<аэрогеле>>.

На рис.??? ...

Полученный результат хорошо описывается механической моделью ...

% мех.модель
% m1 x1'' = - k1 x1 - k3(x1-x2)
% m2 x2'' = - k2 x2 - k3(x2-x1)
%
% m1m2 w^4 - [m1(k2+k3) + m2(k1+k3)]w^2 + k1k2 + k2k3 + k3k1 = 0
%
% k1 = w1^2 m1
% k2 = w2^2 m2
% m1+m2=1


% m1m2 w^4 - [m1m2(w2^2+w1^2) + k3 (m1+m2)] w^2
%   + w1^2 w2^2 m1m2 + k3(w2^2 m2 + w1^2 m1) = 0

% w^4 - [w2^2 + w1^2 + k3/m1/(1-m1)] w^2
%   + w1^2 w2^2 + k3(w2^2 /m1 + w1^2 /(1-m1) ) = 0

% k=0
% mm/(1+mm) w^4 - [mm/(1+mm) (w2^2+w1^2) ] w^2
%   + w1^2 w2^2 mm/(1+mm)  = 0

% w1=w2
% w^4 - [ 2 w1^2 + k3 (1+mm)^2/mm] w^2
%   + w1^4 + k3 w1^2 (1+mm)^2/mm = 0

